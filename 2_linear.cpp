#include<bits/stdc++.h>
using namespace std;

void program1() {
    int arr[] = {6,7,8,4,1};
    int n = sizeof(arr) / sizeof(arr[0]);
    int num;
    

    for (int i = 0; i < n; i++)
    num = arr[i];
    {
        if (arr[i] == num);
    }
    
}